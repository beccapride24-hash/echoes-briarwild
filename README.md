# Echoes of Briarwild

A two-player fantasy adventure prototype built with Phaser, TypeScript, Vite, Colyseus, and WebSockets.

The playable GitHub Pages build deploys automatically from `client/dist`.

> Solo mode runs directly on GitHub Pages. Online co-op requires the included WebSocket server to be deployed separately.
